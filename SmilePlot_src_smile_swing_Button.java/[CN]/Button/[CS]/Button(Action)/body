{
  super(action);
  if (getIcon() != null) {
    String desc=(String)action.getValue(Action.SHORT_DESCRIPTION);
    if (desc == null) {
      desc=getText();
    }
    setToolTipText(desc);
    setText(null);
  }
}
