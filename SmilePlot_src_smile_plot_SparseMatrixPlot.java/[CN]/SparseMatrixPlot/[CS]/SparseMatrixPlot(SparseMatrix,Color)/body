{
  super(color);
  this.sparse=sparse;
  int m=sparse.nrows();
  int n=sparse.ncols();
  x=new double[n];
  for (int i=0; i < x.length; i++) {
    x[i]=i + 0.5;
  }
  y=new double[m];
  for (int i=0; i < y.length; i++) {
    y[i]=y.length - i - 0.5;
  }
}
