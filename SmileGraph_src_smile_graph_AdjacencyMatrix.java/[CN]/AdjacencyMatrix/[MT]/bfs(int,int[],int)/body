{
  cc[v]=id;
  Queue<Integer> queue=new LinkedList<Integer>();
  queue.offer(v);
  while (queue.size() > 0) {
    int t=queue.poll();
    for (int i=0; i < n; i++) {
      if (graph[t][i] != 0.0 && cc[i] == -1) {
        queue.offer(i);
        cc[i]=id;
      }
    }
  }
}
